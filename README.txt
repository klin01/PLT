

COMPILE INSTRUCTION
-i --> interpreter (not used)
-b --> bytecode
-c --> compile into .out
-a --> AST

Example:
./microc -b < tests/test-gcd.mc > test-gcd.b.out

In Shell Script,
Uncomment an indicated part to generate test reference (ONCE code cleaned)