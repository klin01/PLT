Array string $global;
int $x;
Array int $bricks;

function $main: void() {
	Array int $local;
	Brick $b;
	Player $y;
	int $z;

	//$local : new Array int;
	$b : new Brick(0, 0, 0, $bricks, 1, 2);
	$x : 3;
	$z : ($x = 3);
	
	$printint($z);
	//$Push($global, 0);
	/*$Push($global, 5);
	$Push($local, 2);

	$printstring("Should print \"test\" and \"testing\".");
	$printint($global[0]);
	$printint($global[1]);

	//$printstring("Should print 2.");
	//$printint($local[0]);*/
}