Array string $global;
Array int $global2;
int $x;
Array int $bricks;

function $main: void() {
	Array int $local;
	Brick $b;
	Player $y;
	int $z;

	$Push($local, 2);
	$Push($local, 9);
	$Push($global2, 99);
	$Push($global2, 88);
	$Push($global2, 88);
	$global2[9] : 9;
	//$local : new Array int;
	$b : new Brick(0, 0, 0, $local, 1, 2);
	$x : 3;
	$z : ($x = 3);

	$x : $ArrayCount($x);
	
	$printint($x);
	//$Push($global, 0);
	/*$Push($global, 5);
	$Push($local, 2);

	$printstring("Should print \"test\" and \"testing\".");
	$printint($global[0]);
	$printint($global[1]);

	//$printstring("Should print 2.");
	//$printint($local[0]);*/
}